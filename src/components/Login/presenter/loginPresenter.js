const setDatas = (form, errors, handleForm, handleSubmit) => {
  return {
    form: form,
    handle_form: handleForm,
    errors: errors,
    handle_submit: handleSubmit,
  };
};

export { setDatas };
